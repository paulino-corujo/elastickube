log file = /var/log/rsyncd.log
pid file = /var/run/rsyncd.pid
lock file = /var/run/rsync.lock
uid = elasticbox
gid = elasticbox
reverse lookup = no

[elastickube]
    path = /opt/elastickube
    comment = ElasticKube
    read only = no
    list = yes
